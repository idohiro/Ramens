<table class='table'>
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% customers.each do |customerr| %>
      <tr>
        <td><%= customer.name %></td>
        <td><%#= customer.followings.count %></td>
        <td><%#= customer.followers.count %></td>
        <% if current_customer != customer %>
        <% if current_user.following?(customer) %>
        <td><%= link_to "フォロー外す", public_customer_relationships_path(customer.id), method: :delete %></td>
        <% else %>
        <td><%= link_to "フォローする", public_customer_relationships_path(customer.id), method: :post %></td>
        <% end %>
        <% end %>
        <td><%= link_to 'Show', customer, class: "customer_#{customer.id}" %></td>
      </tr>
    <% end %>
  </tbody>
</table>